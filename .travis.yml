language: java
jdk: openjdk12
os: linux
dist: trusty
sudo: required
services:
  - docker
script: 
  - ./gradlew check
  - ./gradlew sonarqube
  - ./gradlew buildDockerImage

before-deploy: 
  skip_cleanup: true 

deploy:
  provider: gae	
  keyfile: serviceaccount.json	  
  project: enduring-honor-256422 